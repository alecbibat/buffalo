<center><h1><%= @question.body %></h1>

<%= form_for [@question, @answer] do |f| %>
  <%= f.text_area :body, placeholder: 'The meaning of life is 42' %>

  <div class='control-group'>
  <%= f.submit "Answer", class: 'btn btn-success' %>
  </div>
<% end %>

</center>